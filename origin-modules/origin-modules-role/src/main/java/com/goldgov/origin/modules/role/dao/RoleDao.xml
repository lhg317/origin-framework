<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goldgov.origin.modules.role.dao.RoleDao">

	<resultMap type="com.goldgov.origin.modules.role.service.Role" id="role">
		<id column="ROLE_ID" jdbcType="VARCHAR" property="roleID"/>
		<result column="ROLE_NAME" jdbcType="VARCHAR" property="roleName"/>
		<result column="ROLE_CODE" jdbcType="VARCHAR" property="roleCode"/>
		<result column="ROLE_TYPE" jdbcType="NUMERIC" property="roleType"/>
		<result column="DESCRIPTION" jdbcType="VARCHAR" property="description"/>
	</resultMap>

	<sql id="queryFields">
		ROLE_ID as "roleID",
		ROLE_NAME as "roleName",
		ROLE_CODE as "roleCode",
		ROLE_TYPE as "roleType",
		DESCRIPTION as "description"
	</sql>
	
	<insert id="addRole">
		<selectKey keyProperty="entityID" resultType="String" order="BEFORE">
		    select uuid() from dual
		</selectKey>
		insert into role (ROLE_ID,ROLE_NAME,ROLE_CODE,ROLE_TYPE,DESCRIPTION)value(#{entityID,jdbcType=VARCHAR},#{name},#{code},#{type},#{description});
	</insert>
	
	<delete id="deleteRoleByIds">
		delete from role where ROLE_ID in
		<foreach item="id" index="index" collection="ids" open="(" separator="," close=")">
		   #{id}
		</foreach>
	</delete>
	
	<update id="updateRole">
		update role 
		<set>
			<if test="name != null">ROLE_NAME = #{name},</if>
			<if test="code != null">ROLE_CODE = #{code},</if>
			<if test="type != null">ROLE_TYPE = #{type},</if>
			<if test="description != null">DESCRIPTION = #{description},</if>
		</set>
		where ROLE_ID = #{id}
	</update>
	
	<select id="findRoleById" resultMap="role">
		select <include refid="queryFields" /> from role where ROLE_ID=#{id}
	</select>
	
	<select id="findRoleList" resultMap="role">
		select <include refid="queryFields" /> from role
	</select>
	
</mapper>